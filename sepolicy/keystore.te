#============= keystore ==============
allow keystore hal_fingerprint_default:dir search;
allow keystore hal_fingerprint_default:file { read open getattr };
allow keystore hal_fingerprint_default:process getattr;